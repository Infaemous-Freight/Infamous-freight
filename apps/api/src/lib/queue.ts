export const anomalyQueue = {
  async add(_name: string, _payload: unknown) {
    return;
  }
};

export const dispatchQueue = {
  async add(_name: string, _payload: unknown) {
    return;
  }
};

export function startWorkers() {
  return;
}

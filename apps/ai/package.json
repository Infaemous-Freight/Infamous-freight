{
  "name": "ai",
  "private": true,
  "scripts": {
    "lint": "echo '(ai) lint not configured'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}

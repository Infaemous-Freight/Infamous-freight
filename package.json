{
  "name": "infamous-freight",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:ai": "pnpm --filter ai dev",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "dev:mobile": "pnpm --filter mobile dev",
    "dev:worker": "pnpm --filter worker dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "secret:scan": "pnpm -r secret:scan",
    "prisma:generate": "pnpm --filter api prisma:generate",
    "prisma:migrate:deploy": "pnpm --filter api prisma:migrate:deploy",
    "repo:check": "node scripts/repo-structure-check.mjs && node scripts/root-readme-check.mjs",
    "repo:bootstrap-readmes": "bash scripts/bootstrap-top-level-readmes.sh",
    "repo:guard:deprecated": "bash scripts/deprecated-root-guard.sh"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}

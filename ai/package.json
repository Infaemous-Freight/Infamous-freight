{
  "name": "ai",
  "version": "0.0.1",
  "private": true,
  "description": "Placeholder for AI services workspace",
  "scripts": {
    "lint": "echo \"(ai) lint not configured\" && exit 1",
    "typecheck": "echo \"(ai) typecheck not configured\" && exit 1",
    "build": "echo \"(ai) build not configured\" && exit 1"
  }
}

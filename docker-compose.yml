version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: infamous-freight-api
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      NODE_ENV: production
      LOG_LEVEL: info
    restart: unless-stopped

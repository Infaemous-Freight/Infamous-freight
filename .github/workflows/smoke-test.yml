name: Smoke Test

on:
  workflow_dispatch:

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Check frontend
        run: curl -I https://www.infamousfreight.com

      - name: Check API
        run: curl -f https://infamous-freight.fly.dev/api/health

      - name: Check proxied API
        run: curl -f https://www.infamousfreight.com/api/health

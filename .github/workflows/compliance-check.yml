name: Compliance Check

on: [push]

jobs:
  compliance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Validate Compliance Policies
        run: ls compliance/policies
